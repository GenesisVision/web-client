import React from "react";

const EmailConfirmSuccess = ({ text }) => {
  return (
    <div className="email-confirm-success">
      <div className="email-confirm-success">{text}</div>
    </div>
  );
};

export default EmailConfirmSuccess;
