// import {
//   FAILURE_SUFFIX,
//   REQUEST_SUFFIX
// } from "shared/reducers/api-reducer/api-reducer";

export const initialState = {
  currentCurrency: "BTC"
};

const walletSettingsReducer = (state = initialState, action) => {
  return state;
};

export default walletSettingsReducer;
